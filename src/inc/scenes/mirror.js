import 'phaser';
/*global Phaser*/

export default class mirror extends Phaser.Scene {
    constructor(mirrorID) {
        super({key: 'mirror'});
        console.log('Running mirror');
        console.log(mirrorID)
        this.localSys = {};
    }
  
    preload() {
        
    }
    
    create() {
        
    }
    
    update() {
        
    }
}